# API Keys for Global Payments
# Replace these sample values with your actual API keys

# API Platform (GP API)
APP_ID=4gPqnGBkppGYvoE5UX9EWQlotTxGUDbs  #gitleaks:allow
APP_KEY=FQyJA5VuEQfcji2M  #gitleaks:allow
PORT=8000  # Optional; Java port is fixed in pom.xml (cargo.servlet.port=8000) and cannot be overridden via this env var